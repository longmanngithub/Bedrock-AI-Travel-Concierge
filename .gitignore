# Python
__pycache__/
*.pyc
.venv/
venv/
.env
.env.*
!.env.example
!*/.env.example

# RAG vector store (regenerate with `python -m app.rag.ingest`)
backend/app/rag/chroma_store/

# Local avatar storage fallback (runtime data, not source)
backend/data/

# Evaluation harness + outputs (not pushed to GitHub)
backend/evaluation/

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/.next/
frontend/out/
frontend/.turbo/
frontend/*.tsbuildinfo
frontend/npm-debug.log*

# Docker
*.pid
docker-compose.override.yml

# GCP service-account key (Vertex AI auth for production deploy)
gcp-key.json
*.gcp-key.json

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code config (not pushed to GitHub)
.claude/

# Project report (not pushed to GitHub)
report/

# Claude Code project instructions (not pushed to GitHub)
CLAUDE.md

# Deployment
DEPLOYMENT.md
