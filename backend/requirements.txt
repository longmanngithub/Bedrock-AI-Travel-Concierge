# --- Core Agentic stack ---
crewai>=1.15.0            # multi-agent orchestration
google-genai>=1.0.0      # native Gemini provider used by CrewAI 1.x
litellm>=1.72.0          # provider-agnostic LLM routing (Groq / Ollama / others)

# --- LangChain (tools + RAG) ---
langchain>=0.3.0
langchain-community>=0.3.0     # DuckDuckGo search tool
langchain-chroma>=0.2.0        # Chroma vector store integration
langchain-huggingface>=0.2.0   # local sentence-transformers embeddings
sentence-transformers>=3.0.0
chromadb>=0.5.0
ddgs>=6.0.0                     # DuckDuckGo search backend (no API key)

# --- Web API + persistence ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
slowapi>=0.1.9              # per-IP rate limiting on the LLM-backed endpoints

# --- Utilities / evaluation ---
requests>=2.32.0
pandas>=2.2.0
tabulate>=0.9.0
