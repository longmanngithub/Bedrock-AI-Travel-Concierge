# --- Core Agentic stack ---
crewai>=1.15.0            # multi-agent orchestration
google-genai>=1.0.0      # native Gemini provider used by CrewAI 1.x
litellm>=1.72.0          # provider-agnostic LLM routing (Groq / Ollama / others)

# --- LangChain (tools + RAG) ---
langchain>=0.3.0
langchain-community>=0.3.0     # DuckDuckGo search tool
langchain-chroma>=0.2.0        # Chroma vector store integration
langchain-huggingface>=0.2.0   # local sentence-transformers embeddings
sentence-transformers>=3.0.0
chromadb>=0.5.0
ddgs>=6.0.0                     # DuckDuckGo search backend (no API key)

# --- Web API + persistence ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
pydantic>=2.7.0
pydantic[email]>=2.7.0    # EmailStr validation for auth
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
slowapi>=0.1.9              # per-IP rate limiting on the LLM-backed endpoints
alembic>=1.13.0            # database migrations (replaces the create_all hack)

# --- Background jobs (crew runs off the request thread) ---
arq>=0.26.0                # asyncio-native Redis job queue
redis>=5.0.0              # progress streams + queue backend

# --- Auth ---
argon2-cffi>=23.1.0       # password hashing (Argon2id)
PyJWT>=2.8.0              # short-lived access tokens (HS256)
authlib>=1.3.0           # Google OAuth (Authorization Code + PKCE)
httpx>=0.27.0            # OAuth token/JWKS fetches
certifi>=2024.2.2        # explicit CA bundle for PyJWKClient (stdlib urllib
                          # doesn't get httpx's bundled certifi automatically)

# --- Delivery (Phase 4: PDF + email + Telegram) ---
weasyprint>=62.0        # HTML->PDF boarding-pass rendering (needs system Pango/
                          # cairo/gdk-pixbuf — see Dockerfile / README on macOS dev)
# Exact pins: avatar upload validation, multipart parsing, and R2's supported
# S3-compatible API. Keep them pinned for reproducible security updates.
boto3==1.35.99
Pillow==12.3.0
python-multipart==0.0.20

# --- Utilities / evaluation ---
requests>=2.32.0
pandas>=2.2.0
tabulate>=0.9.0
